# Birthday 数字收藏卡 · 视觉设计方案(v1)

> 工作台定位:客户提供照片 + 生日信息 → 工作台生成数字收藏卡 → 打包给客户 → 客户打开网页查看。
> 本方案只专注「Birthday 主题的卡牌设计系统」,不涉及小程序/商城/支付/NFT/抽卡。

---

## 〇、总原则

**CARD DESIGN FIRST. HOLOGRAPHIC EFFECT SECOND.**
全息是卡面的一种特殊材质,不是卡牌的主体设计。

- 静止时:一张漂亮、克制、有收藏感的印刷卡
- 轻微倾斜:边缘出现很轻的金属光泽
- 中等倾斜:局部柔和虹彩
- 大角度:明显但高级的 Holo 反射 + 边缘高光 + 少量 sparkle
- 回正:效果逐渐消失

禁止:静止大面积彩虹、固定彩虹斜纹、霓虹边框、整卡持续发光、Holo 盖过照片。

**照片永远第一主角**;卡框、文字、星点、Holo 都只是辅助。

---

## 一、四套统一品牌元素(必须一致)

| 项目 | 统一规则 |
|---|---|
| 卡牌比例 | 1024×1536(≈5:7.5,引擎既定几何,保证零拉伸) |
| Card ID | `CARD #0001` 逻辑与数据结构完全一致,正面 + 背面都出现 |
| 图层结构 | 照片层 / 照片调整 / 背景层 / 渐变光层 / 装饰层 / 排版层 / 卡框层 / Holo 层(八层拆分) |
| 背板信息体系 | 顶部 `HAPPY BIRTHDAY` + `CARD #`;中部年龄水印 + 卡名;下部凭证区:卡名 / 日期 / 祝福 / `CREATED BY` / `OWNED BY` |
| 字体体系 | 衬线 = Palatino Linotype(纪念感);无衬线 = Segoe UI(信息);黑体 = Bahnschrift(Pop 专属) |
| 边框逻辑 | 都是"极细、发丝级"的卡牌边缘,不是网页组件;不同模板只变线宽/线色/角饰 |
| Holo 交互 | 四套共用同一套门控参数(静止 15% → 大角度 100%) |
| 数字收藏品定位 | 都保留 Card ID、编号、归属字段 —— 不是游戏卡、不带属性 |

**四套的差异必须体现在**:构图 / 字体比例 / 照片位置与处理 / 标题位置 / 装饰方式 / 背景处理 / 年龄数字用法 / 边框形式 / 光影语言。

---

## 二、Birthday / Celebration(明亮开心 · 主力销售款)

**设计理念**:生日当天的快乐感,但用"年轻高级生活方式"的语言表达,而不是儿童贺卡。照片像被彩纸包围的一张快照。

| 项目 | 方案 |
|---|---|
| 配色 | 奶油白 `#FBF6EC` / 柔黄 `#F6D27A` / 暖橙 `#F0A868` / 珊瑚粉 `#E8796A` / 少量天蓝 `#7FB2D9`;文字墨色 `#3A342C` |
| 构图 | 照片**从卡顶出血**占上方 ~63%,底部两角大圆角(radius 132);下半部为奶油白文字区,左侧大数字、右下信息 |
| 照片处理 | 提亮 1.06 / 对比 1.06 / 饱和 1.06 / 轻微暖调 / 暗部抬升 / 底边 30% 渐变融入奶油白 / 边角轻微虚化 |
| 字体 | 数字:Sans Black(粗几何);主标题:Palatino 衬线;信息:Segoe UI |
| 装饰 | 柔黄/珊瑚粉/天蓝的**小圆点、短纸屑、小星芒**(约 34 个,避开人脸)、3 个空心细圆、色块顶边 |
| 边框 | 墨色 2px 圆角矩形(radius 40)+ 左上珊瑚粉星芒 + 右下柔黄星芒 |
| 年龄数字 | **主角级**:232px 粗体珊瑚粉数字 + 柔黄错位阴影,右上角小号 `TH`;压在"照片→奶油白"的过渡带上 |
| 正面布局 | 顶部居中 `HAPPY BIRTHDAY`(照片上,白字带软阴影)+ 可选 `FOR 名字`;左下大数字 + `YOUR DAY` + 一行短句;右下日期 + `CARD #` + 三颗彩色圆点 |
| 无年龄时 | 大号年份 `2026` + `BIRTHDAY` 替代,不虚构年龄 |
| 背面布局 | 奶油白底 + 柔黄/珊瑚粉光晕,墨色圆角细框 + 两枚星芒;结构与全系列一致 |
| Holo | 光泽 0.50;浅色卡面上表现为**淡香槟金属反光**,静止几乎不可见 |

---

## 三、Birthday / Soft(温柔浪漫 · 摄影纪念页)

**设计理念**:像高级生活方式杂志里的生日纪念页 —— 照片是作品,留白是呼吸,金线是装裱。

| 项目 | 方案 |
|---|---|
| 配色 | 米白 `#F7F2EA` / 奶油 `#EFE7DA` / 香槟金 `#D8C3A0` / 极淡粉 `#E8D2CE`;文字暖褐 `#5B5248` |
| 构图 | 照片**全宽、无硬边**,从卡顶延伸至约 65% 高度后**长达 55% 的渐变融入米白**;下半部大幅留白,左侧细金圆 + 年龄,右侧文字块,中间一条竖直发丝金线分隔 |
| 照片处理 | 降饱和 0.86 / 提亮 1.05 / 暗部抬升 / 边缘更强虚化(3.4) / 无暗角,通篇柔和 |
| 字体 | 全衬线 Palatino(主标题 66px)+ Segoe UI 小号信息 |
| 装饰 | 3 个半透明柔粉大圆(极低透明度)、9 片极小花瓣、1 枚金色小星芒、1 个细金圆环 |
| 边框 | **双线香槟金**(2px + 1px)+ 四角小圆点(无星芒) |
| 年龄数字 | **精致化**:84px 衬线数字嵌在一枚细金圆环内,下方小字 `YEARS` |
| 正面布局 | 顶部居中 `HAPPY BIRTHDAY`(照片渐变区上,暖白字软阴影)+ 可选 `FOR 名字`;左下金环数字;右侧 `YOUR DAY` → 短句 → 日期 → `CARD #` |
| 无年龄时 | 移除金环数字,主标题上移,保持留白版式 |
| 背面布局 | 米白"博物馆标签"式:双金线框 + 居中凭证区,留白最大 |
| Holo | 光泽 **0.38(全系最低)**;只在倾斜时出现一层若有似无的珠光 |

---

## 四、Birthday / Pop(年轻大胆 · 视觉最强)

**设计理念**:年轻设计品牌 / 时尚杂志的排版冲击力。年龄数字就是画面主体,照片被"设计"进色块里。

| 项目 | 方案 |
|---|---|
| 配色 | 亮蓝 `#2B4CFF` / 黄 `#FFD400` / 红 `#FF4B3E` / 粉 `#FF9AC1` / 橙 `#FF7A2F`;纸白 `#FFFDF6`;墨黑 `#141414` |
| 构图 | 照片全宽占上方 ~71%,**底边斜切**(左下低、右上高)接蓝色大色块;顶部黄条 + 红短条;右下粉圆 + 半调点阵 |
| 照片处理 | 对比 1.16 / 饱和 1.14 / 暗部压制 1.12 / 底部 34% 渐入亮蓝 / 边缘轻虚化 |
| 字体 | **Bahnschrift 粗黑体**贯穿;`BIRTHDAY` 116px,年份 54px,信息 20–22px |
| 装饰 | 彩色方块、大号黑色星芒(约 10 个)、粗短线组、粗弧线、半调点阵 |
| 边框 | 3px 纯黑直角细框(与色块形成"印刷出血"关系) |
| 年龄数字 | **最大胆**:296px 黑体数字 + 黄色错位阴影 + 右上大写 `TH`;数字**横跨照片与蓝色块**,是构图核心 |
| 正面布局 | 左上 `HAPPY BIRTHDAY`(粗黑);左下巨大数字 + `YOUR DAY` + 可选 `FOR 名字`;右下日期 + 黑底白字 `CARD #` 胶囊 |
| 无年龄时 | 用 `BIRTHDAY`(116px)+ 年份(54px,黄色)承担主体,不虚构年龄 |
| 背面布局 | 纸白 + 黄条 + 红短条 + 底部蓝色块;黑色粗框;巨大 `BIRTHDAY` 字标;结构与全系列一致 |
| Holo | 光泽 0.60;在彩色卡面上作为"特殊油墨"出现,倾斜时边界与数字处最先亮起 |

---

## 五、Birthday / Night(高级安静 · 系列高级款)

**设计理念**:成熟、克制、像深夜灯光下的收藏卡。照片与深色底**没有边界**,只有光影过渡。

| 项目 | 方案 |
|---|---|
| 配色 | 深蓝 `#0A0F1C` / 黑 / 深灰 / 香槟金 `#DEC9A0` / 少量暖粉 `#D9A6A0` |
| 构图 | 照片全宽带(约 y118–1232),**四边羽化**(上 150 / 侧 78 / 下 340);下半部渐次融入深蓝,直到完全消失;大号暖金数字压在过渡带上 |
| 照片处理 | 提亮 0.97 / 对比 1.07 / 降饱和 0.90 / 暗部压制 + 冷蓝阴影 / 局部景深 / 暗角 0.42 |
| 字体 | Palatino 衬线(主标题 / 数字)+ Segoe UI(信息) |
| 装饰 | 细星点、十字小星芒、极少量金属碎屑、柔光斑、两道细弧(占画面 0.66%) |
| 边框 | **双线香槟金**(2px + 1px)+ 四角短金线 + 四角星芒 |
| 年龄数字 | 182px 暖金衬线数字 + 3px 深金雕刻偏移,右上小星芒 |
| 正面布局 | 顶部居中 `HAPPY BIRTHDAY` + 细线星芒;左侧大数字 + `YOUR DAY` + 极淡短句;右侧日期 + `CARD #` |
| 无年龄时 | `YOUR DAY` 回归 104px 主标题 |
| 背面布局 | 深蓝渐变 + 拉丝纹理 + 极淡圆环;双金线框 + 四角星芒;年龄做 8.5% 透明水印;凭证区字段齐全 |
| Holo | 光泽 0.52;深色底上虹彩最容易被"读"出来,但仍只在倾斜后出现 |

---

## 六、程序化实现(批量生产可行)

**八层 → 引擎贴图槽映射**(不需要改 3D/Parallax/Holo 底层):

| 设计层 | 引擎实现 | 视差表现 |
|---|---|---|
| 1 Photo | `subject.png`(照片,含裁切/调色/融合) | 深度 0.30–0.36(中层) |
| 2 Photo Adjustment | 烘焙进 `subject.png` | — |
| 3 Background | `background.png` | 深度 −0.24 ~ −0.30(最远) |
| 4 Gradient / Light | 烘焙进 `background.png` | — |
| 5 Decorative | `effects.png` | 深度 0.55–0.66(最近) |
| 6 Typography | `text.png`(最前,不参与视差) | 固定 |
| 7 Frame | `text.png` 同一层 | 固定 |
| 8 Holo | 查看器 GLSL(四套共用) | 视角门控 |

**批量公式**:`照片X + 模板Y → 卡XY`。四张卡视觉完全不同,但层结构、参数体系、Card ID、背板字段完全一致 —— 换照片不用改任何代码。

**构建命令**
```bash
python card-studio/build_birthday_set.py "照片.jpg"     # 一次生成四套
cd birthday-set && node server.mjs                      # 打开 http://127.0.0.1:4185
```

**可配置字段**(每套独立 `card-config.json`):
`title`(卡名) `subtitle`(HAPPY BIRTHDAY) `age`(留空则不出现,绝不虚构) `name`(名字/昵称,留空则不出现)
`technique`(日期) `edition`(CARD #XXXX) `tagline` `wish`(祝福) `createdBy` / `ownedBy`(留空显示占位细线)
`appearance.finish`(pearl/银箔/烫金/原画) `backStyle` `parameters.*`(各层深度与光泽)

---

## 七、本阶段不做(明确排除)

小程序、商城、用户收藏馆、支付、社交、NFT、交易、TCG 属性(HP/Attack/Skill)、抽卡机制。

---

## 八、当前实机结果(同一张真实照片)

| 模板 | 照片占比 | 排版层占比 | 装饰占比 | Holo 光泽 |
|---|---|---|---|---|
| Celebration | 61.3% | 2.51% | 0.37% | 0.50 |
| Soft | 53.4% | 1.56% | 6.59%(极低透明度柔光) | 0.38 |
| Pop | 64.1% | 3.83% | 0.55% | 0.60 |
| Night | 56.7% | 1.94% | 0.66% | 0.52 |

- 总览(四张并排):`http://127.0.0.1:4185/`
- 单卡:`/celebration/` `/soft/` `/pop/` `/night/`
- 每套四态(正面静止 / 轻微倾斜 / 大角度 Holo / 背面):`/<模板>/showcase.html`

> 所有卡均使用同一张原始照片:仅裁切 / 调色 / 暗部处理 / 渐变融入,**未抠图、未重绘、未改变人物**。

---

## 九、待确认与可选优化

1. 四套中哪一套作为**首发主力**(建议 Celebration / Night 双主线)。
2. 是否需要**名字/昵称**上正面(现在仅 Celebration / Soft / Pop 在提供时显示,Soft 为 `FOR 名字`)。
3. 卡牌比例是否要改成**精确 5:7**(需同步调整 3D 几何,一次改动,四套同时生效)。
4. 背板 `CREATED BY` / `OWNED BY` 是否接入实际署名规则(如店铺号)。
5. 是否需要每套模板再出**浅/深两个变体**(同构图换明度体系)。
