# 全息收藏卡系统 · 介绍文档

> 版本:2026-09 · 工作目录:`D:\陈大帅\ai搞一搞\虚拟产品项目\卡片card`
> 一句话:把你的一张图,变成一张**有内部空间纵深、全息克制的数字收藏卡网页**,同时交付可编辑 Blender 工程与分层素材。

---

## 一、这套系统是什么

一条**全程序化**(不依赖 AI 生图)的卡片生产线,由三部分组成:

1. **生成引擎**(RuiC-card-skill):把你准备的图 + 文案,做成"四层合成 + 3D 场景"的卡片,并导出网页。
2. **卡片工坊**(card-studio):一个本地网页工具(端口 4399),你**拖图 → 填几个字 → 点生成**,剩下的全自动。
3. **查看器**(web-template):卡片成品页面——拖转、翻面、调景深/光泽、切换卡面质感、保存图片。

产出物包含:**可拖转的全息卡网页 + 可编辑 `card.blend` + 分层透明 PNG + 配置 JSON**。

---

## 二、目录结构与各自作用

```
卡片card\
├── RuiC-card-skill-main\          # 生成引擎(手动安装的 Codex Skill, 纯代码+文本)
│   ├── SKILL.md                   # 操作手册
│   ├── scripts\
│   │   ├── ensure_blender.py      # 自动获取官方 Blender 便携版 + SHA-256 校验
│   │   ├── build_card.py          # 建可编辑 3D 视差场景(存 card.blend)
│   │   ├── export_web.py          # 导出卡片几何 card.glb(含浏览器材质契约名)
│   │   ├── generate_typography.py # 楷体文字层(透明 PNG)排版
│   │   ├── validate_assets.py     # 素材体检(尺寸/真透明/线稿对比度)
│   │   ├── checkerboard_to_alpha.py # 棋盘格假透明 → 真 Alpha(确定性算法)
│   │   └── run_pipeline.py        # 一键流水线
│   └── assets\web-template\       # 查看器模板(每次生成都从这里复制)
│       ├── index.html  style.css  app.js  app.bundle.js  server.mjs  icons.data.js
│
├── demo-koi\                      # 示例卡:水墨锦鲤
│   ├── assets\                    # subject / background / lineart / text / effects / source
│   ├── card.blend                 # 可编辑工程
│   ├── web\                       # 卡片网页(可独立起服务)
│   └── tools\blender-4.5.13-windows-x64\   # 共享的便携 Blender(约 1.2GB, 只此一份)
│
└── card-studio\                   # 卡片工坊(本地工具)
    ├── 启动卡片工坊.bat            # 双击启动(起服务 + 开浏览器)
    ├── 使用说明.md                 # 极简上手指引
    ├── server.mjs                 # 本地服务: 任务队列 / 日志 / 预览路由 / 端口 4399
    ├── public\index.html          # 拖放界面
    ├── prepare.py                 # 图片处理: 判断素材→抠图/保留→摆放→配背景与线稿→写配置
    ├── make_effects.py            # 前景层生成器(星尘/光点/星芒/光带)
    ├── shader_patch.py            # 查看器 shader 调优补丁(纵深+克制全息)
    ├── apply_depth_demo.py        # 把纵深/全息改动应用到指定 Demo
    ├── add_labels.py / add_swatch_labels.py  # 给预览页按钮/质感圆点加中文标注
    ├── _shader_backup\            # 原版 shader 备份(可回滚)
    └── projects\card-xxxx\        # 每张卡一个独立目录(产出见第七节)
```

---

## 三、使用方式

### 启动
- **推荐**:双击 `card-studio\启动卡片工坊.bat` → 自动开服务并打开 `http://127.0.0.1:4399`;关掉黑窗即停止。
- 也可以命令行:`cd card-studio` → `node server.mjs`。

### 生成一张卡(三步)
1. **拖图**:把 PNG/JPG/WebP 拖进页面(或点击选择)。
2. **填文字**(可全空):卡名、副标题、技法/招式、一句话、编号、系列名、背面描述。
   - 卡名留空 → 自动取文件名;编号留空 → 自动递增 `NO.001 / 001`。
3. **选风格**:
   - 图片处理方式:`自动判断` / `保留整图(不抠)` / `扣除纯色底`
   - 背景风格:`宣纸墨韵` / `深空星夜` / `玄黑烫金` / `素雅米白`
   - 点「生成全息卡」→ 约 1~3 分钟 → 页面下方直接预览。

### 预览与调节
- 拖动 = 旋转卡片;按钮(带文字):**自动 / 翻转 / 重置 / 调整**
- 「调整」弹出景深面板:画面比例、画面景深、底纹景深
- 右下角卡面质感:**珠光 / 银箔 / 烫金 / 原画**;旁边「光泽」滑块控制全息强弱
- 右上「保存」可直接把当前卡面存成图片
- 预览上方「↗ 在新窗口打开预览」→ 全屏调节更舒服

### 其他
- 生成历史在「③ 任务与结果」里;**服务重启后会自动从磁盘恢复**,不会丢列表。

---

## 四、图片是怎么被处理的(核心逻辑)

所有处理都在 `prepare.py`,画布固定 **1024 × 1536**(竖版 3:2 卡面)。

### 4.1 三种处理方式

| 方式 | 触发条件 | 行为 |
|---|---|---|
| **透明/棋盘格 → 抠出主体** | 图片本身有透明通道(透明像素 > 0.2%),或检测到棋盘格底 | 直接当主体;棋盘格用确定性算法(识别格子周期与两种底色,逐像素算距离)转成真 Alpha,**不是 AI 猜的** |
| **扣除纯色底** | 四边颜色波动 ≤ 10(即真的纯色底) | 保守抠图:容差 `max(5, min(14, spread+3))`,只去掉与边角**几乎同色**的像素 |
| **保留整图(不抠)** | 以上都不是(复杂背景),或你手动选择 | 图片**每个像素原样保留**,完整缩放上卡、零裁切,两侧用所选风格衬底 |

> 设计原则(按你的要求定):**绝不强行抠复杂背景**。自动模式下遇到非纯色底,会自动落到"保留整图",只提示不硬抠;手动选「扣除纯色底」才会强制尝试,失败则明确报错让你换图。

### 4.2 保守抠图的完整流程
1. 缩到最长边 480px(提速),取四边最外 2px 的颜色分布,算中位数与波动 `spread`。
2. 容差随底色纯净度收紧:`tol = max(5, min(14, spread + 3))` → 浅色衣物、近白细节**不会被误伤**(实测:纯白底 + 一个 `#EEEEEE` 浅灰圆,白底被抠掉、浅灰圆完整保留)。
3. 从四边做**连通填充**(BFS 四邻域),只在与边角背景色相连的区域判为背景。
4. 质量闸门:主体占比必须在 5%~97% 之间,否则判失败。
5. 收尾:蒙版向内收缩 1px(去白边)+ 羽化 1.0px(边缘不硬)。

### 4.3 摆放规则
| 模式 | 构图区 | 中心 | 说明 |
|---|---|---|---|
| 抠图主体 | 900 × 980 | (512, 762) | 避开上下 15% 文字区 |
| 保留整图 | 940 × 960 | (512, 768) | contain 缩放,不裁切 |

缩放统一用 Lanczos;放大上限 2.0 倍(太糊的图不强拉)。

### 4.4 另外三层怎么来
- **背景**:纯程序化绘制(纸纹/墨迹/星点/烫金颗粒等),四种风格各自色板。
- **线稿**:
  - 抠图模式 = **主体轮廓描边**(用主体 Alpha 做膨胀相减),与主体**逐像素对齐**,转动不会错位;
  - 整图模式 = 画面内部高对比边缘描线 + 画幅细边框。
- **文字**:`generate_typography.py` 用系统**楷体**(simkai)等宽排布,输出透明 PNG;卡面文字层位于**最前方**。

---

## 五、卡牌内部的四层空间纵深(本次重点)

这是与"整张卡在转"最大的区别:**每一层有自己的 Z 深度,拖动时位移幅度各不相同**。

| 顺序 | 图层 | 深度参数 | 拖动时的表现 |
|---|---|---|---|
| 1(最远) | 背景 | `backgroundDepth = -0.28 ~ -0.30` | 反向缓移,退到最里 |
| 2 | 主体(人物/照片/画作) | `subjectDepth = 0.34` | 中幅移动 |
| 3 | **前景:星尘/光点/星芒/光带** | `effectsDepth = 0.60 ~ 0.62` | 位移最大,明显浮在卡前 |
| 4(最前) | 卡框 + 文字 | 文字层**不参与视差** | 几乎纹丝不动,牢牢贴面 |

底层实现:`parallax(uv, depth) = uv + 视线方向.xy / |视线方向.z| × depth × 系数`,每层按自己的 depth 采样贴图。
**系数已从 `0.10` 提到 `0.20`**,层间错开幅度翻倍,所以纵深一眼可辨。

前景层由 `make_effects.py` 生成(每张卡可选):150 颗细星点 + 26 个大颗虚焦光斑 + 20 个十字星芒 + 7 条柔光带,全部加起来只占画面 **1.31%** 可见像素——有空气感,但不遮主体。

---

## 六、全息材质:降存在感 + 视角门控

目标:**正常看是一张漂亮的印刷收藏卡,全息只是材质;转动时才亮起来。**

| 项目 | 调整前 | 调整后 | 效果 |
|---|---|---|---|
| 全息强度随视角 | 恒定全量 | **静止 15% → 倾斜 100%**(门控) | 静止干净,转动渐显虹彩 |
| 彩虹条纹压暗量 | `.21` | `.11` | 大面积彩虹不再是主体 |
| 条纹提亮量 | `.065 + .11` | `.028 + .06` | 光泽更含蓄 |
| 边缘高光 | `.42 / .3` | `.22 / .16` | 只在边缘微微镶光 |
| sparkle 阈值/强度 | `step .994` / `.13` | `step .9975` / `.08` | 从"满屏闪"变"少量星点" |
| 线稿辉光 | `.055` | `.035` | 描边发光更淡 |
| 默认光泽 `foil` | `0.60` | **`0.40 / 0.42`** | 出厂即克制,可用滑块再调 |

转动时会依次出现:**虹彩相位偏移 → 边缘高光 → 局部 foil shine → 少量 sparkle**。
四种卡面质感仍在:珠光(彩虹)/ 银箔(冷银)/ 烫金(暖金)/ 原画(关闭全息)。

---

## 七、每张卡的产出

生成目录 `card-studio\projects\card-xxxx\`:

| 文件/目录 | 内容 |
|---|---|
| `web\` | 卡片网页(`index.html` / `style.css` / `app.bundle.js` / `assets\`),可独立部署 |
| `card.blend` | **可编辑 Blender 工程**(视差层、全息节点、灯光、合成辉光) |
| `assets\subject.png` | 主体层(透明) |
| `assets\background.png` | 背景层(不透明) |
| `assets\lineart.png` | 线稿层(白底黑线) |
| `assets\text.png` | 文字层(透明) |
| `assets\effects.png` | 前景星尘层(可选) |
| `assets\source.png` | 你上传的原图(留底) |
| `card-config.json` | 全部文案与参数(改完重跑即可出新版) |
| `meta.json` / `job.log` | 生成记录与日志(排错用) |

预览地址:工坊页 `http://127.0.0.1:4399/p/card-xxxx/`;
想让某张卡**独立运行**:`cd projects\card-xxxx\web` → `node server.mjs`(默认 4173)。

---

## 八、参数速查(想自己微调时看这里)

`card-config.json` → `parameters`:

| 键 | 含义 | 现值(Demo) | 可调建议 |
|---|---|---|---|
| `subjectScale` | 主体画面比例 | 1.02 / 1.18 | 0.95~1.35 |
| `subjectDepth` | 主体深度 | 0.34 | 调大更"凸" |
| `backgroundDepth` | 背景深度 | -0.28 / -0.30 | 越负退得越远 |
| `effectsDepth` | 前景深度 | 0.60 / 0.62 | 0.8 会非常"飘" |
| `effectsScale` | 前景缩放 | 1.04 / 1.05 | 1.05~1.15 |
| `foil` | 全息光泽强度 | 0.40 / 0.42 | 0 = 全关(同"原画") |

`appearance`:`finish`(gold/silver/pearl/original)、`background`(页面底色)
`safeArea`:`scale` / `offset`(文字安全区的映射,防止卡面内容压到文字)

全局观感(影响所有卡)在 `RuiC-card-skill-main\assets\web-template\app.js` 与 `app.bundle.js`(两份必须同步改):
视差系数 `.20`、视角门控 `mix(0.15, 1.0, tilt)` 等;改完复制到各项目 `web\` 下即可。

---

## 九、技术链路(生成一次卡都发生了什么)

```
拖图 → prepare.py
   ├─ 判断素材(透明/棋盘格/纯色/复杂)
   ├─ 抠图或整图保留 → 摆放进 1024×1536
   ├─ 生成背景(所选风格)
   ├─ 生成线稿(轮廓/边缘)
   └─ 写 card-config.json
        ↓
run_pipeline.py
   ├─ validate_assets.py      素材体检(尺寸一致/真透明/线稿对比度)
   ├─ ensure_blender.py       找或装官方便携 Blender(SHA-256 校验)
   ├─ build_card.py           Blender 建视差场景 → card.blend
   ├─ export_web.py           导出 card.glb(材质契约名 web_front/edge/back/gold)
   └─ 复制 web-template + 写 web/card-config.json + 拷贝分层图
        ↓
node web/server.mjs 或工坊预览路由
        ↓
浏览器:Three.js 单文件查看器(纹理全部内联打包,零外部依赖请求)
```

要点:
- **glTF 传不了 Blender 的自定义节点图**,所以网页端用**同一套 UV 公式 + GLSL** 重建材质,观感一致但非逐像素相同。
- 查看器打包成**单文件**(three.js 与图标全部内联),广告拦截插件无从下手;浏览器若关闭硬件加速,还有 **CSS-3D 分层兜底**,不会白屏。

---

## 十、已做的可用性修复(工坊与预览页)

- 预览页底部四个工具按钮**被白色块遮挡** → 工具条改为**最高层级不透明白色药丸**(`z-index:60` + 描边阴影),并给画廊加叠层隔离;虚浮文字层设为不拦截点击。
- 四个按钮**加中文标注**:自动 / 翻转 / 重置 / 调整。
- 四个质感圆点**加文字**:珠光 / 银箔 / 烫金 / 原画。
- 「调整」浮层加了高度上限与滚动,矮窗口不再被切掉。
- 工坊预览区加高到 82vh,并增加**新窗口打开**按钮。
- 工坊任务列表**重启自动恢复**(扫描 `projects\` 目录)。
- 修复引擎脚本的一个小瑕疵:找不到 `bun` 时不再报错中断(直接用模板预打包文件)。

---

## 十一、常见问题

**Q:提示"未能安全抠出主体"?**
背景不纯或主体贴边。换透明底 PNG / 纯色底图,或在处理方式里选「保留整图」。

**Q:提示"找不到 Blender"?**
需保留 `demo-koi\tools\blender-…`(共享的那份);或把 Blender 目录放到工作区 `tools\blender-…\`。

**Q:卡面文字用什么字体?**
默认楷体(`C:\Windows\Fonts\simkai.ttf`)。想换字体,可在 `card-config.json` 加 `"font": "字体文件路径"` 后重新生成文字层。

**Q:复杂照片背景能不能抠?**
能,但需要"干净底"这个前提。照片级抠图属于 AI 抠图范畴,本系统刻意不引入(全程序化),因此默认保留整图。

**Q:能批量做很多张吗?**
可以一张一张拖;要做批量流水线(读文件夹批量出卡)需要我再加一个批处理入口。

**Q:能商用/二次修改吗?**
卡片内容(你的图、Blender 工程、网页)都在你自己的目录里,随便改;生成引擎来自开源 skill(RuiC-card-skill,保留其 LICENSE)。

---

## 十二、当前限制与可扩展方向

**限制**
- 线稿是"轮廓/边缘描边",不是手绘精修线稿。
- 自动抠图只对透明/棋盘格/纯色底可靠。
- 全息与纵深属于视觉调优,需要你在浏览器里最终确认观感(生成端无法自动"看"效果)。

**可扩展(想做随时说)**
1. 把前景星尘层**接进工坊流程**,以后每张新卡自动带(现在是 Demo 专属)。
2. 增加背景风格(赛博霓虹、金箔屏风、胶片颗粒…)。
3. 批量生成:选定文件夹 → 自动出整套卡 + 编号递增。
4. 每张卡可选择"层深预设":克制 / 标准 / 夸张纵深。
5. 导出短视频(自动旋转一圈的 MP4)用于商品详情页。
6. 卡背设计器(背面文案 + 图案)。

---

*文档随功能更新;最近一次更新包含:四层纵深体系、全息视角门控、前景星尘层、工坊 UI 修复。*
