# archive / v1-birthday —— 旧方向归档(只读参考)

**这里的东西不再决定项目方向。** 现行规范见 [`../../docs/V2-主规范.md`](../../docs/V2-主规范.md)。

## 为什么归档
V2 方案(2026-09-16)判定:上层产品逻辑仍被多个历史阶段混合影响 ——
旧 Birthday 五套模板、旧字段表单、"全程序化"旧定位、全息/景深参数、
以及"只会选色的 AI 辅助"仍在支配工作台。因此**保留底层技术能力,归档错误的产品入口**。

## 内容
| 路径 | 是什么 | 状态 |
|---|---|---|
| `docs/Birthday系列-设计方案.md` | v1 生日模板系列设计方案 | 归档,**不再作为规范** |
| `docs/数字收藏卡项目_AI完整开发提示词-v1.docx` | v1 给开发 AI 的提示词(含旧小程序状态等过时事实) | 归档,已被 `docs/V2-主规范.md` 取代 |
| `docs/卡片工坊-介绍-v1.md` | v1 工坊介绍(全程序化 / 拖图→填文字→选风格) | 归档,根目录已重写为 V2 版 |
| `legacy-cards/night-premium/`、`legacy-cards/night-user/` | 早期阶段的成品卡残留(已确认**无任何代码引用**) | 只读保留 |

## 仍然保留可运行的部分(不是归档,是兼容层)
- `card-studio/birthday_system.py` 及五套模板渲染:保留为 **legacy renderer**,旧卡与新卡都能重出
- 旧 Card Package 与 Showcase:**必须仍能打开**(V2 硬性约束之一)

## 使用原则
1. 不要把这里的文档当作现行产品规范
2. 不要基于这里继续新增 Birthday 模板开发
3. 需要参照历史设计时可以参考,但**与 `docs/V2-主规范.md` 冲突时以主规范为准**
