# V2 进度日志(每次改动都追加,防止后续跑偏)

> **规矩**:每个阶段结束都要在这里追加一条 —— **改了什么文件 / 为什么 / 怎么运行 / 测试结果 / 已知问题**。
> 与 `V2-主规范.md` 冲突时,以主规范为准;若主规范需要改,先在此记录"决策变更"再改规范。
> 最近更新:**2026-09-16**

---

## 0. 已定决策(不可随意推翻)
| # | 决策 | 说明 |
|---|---|---|
| D1 | **以《V2 最终整改方案》为主** | 旧冲突一律按文档改;旧 Birthday 方向不再主导 |
| D2 | **AI 必须真正承担设计职责** | 不做成"只选色/调参数";目标是 Art Director 出 3~4 个实际提案 + Visual Critic 自检 |
| D3 | **设计语言 = PORTRAIT / CYBER / EDITORIAL / MEMORY** | 每套先做 1 个标杆款;Cinema 不再作为独立语言(其能力并入 EDITORIAL/记忆方向) |
| D4 | **字段精简** | 只留 姓名 / 纪念日期 / 客户文案 / 备注;**Card ID 系统自动生成** |
| D5 | **五套 Birthday 降级** | 保留可运行资产与兼容路径,但不再是产品核心入口,停止新增开发 |
| D6 | **旧文档归档** | `archive/v1-birthday/`;README / 介绍 / AI 提示词重写为 V2 |
| D7 | **正面自由 Artwork;背面统一 Card Identity** | 背面必选 Card ID + Date,可选字段空则隐藏 |
| D8 | **Card Package 是三端契约** | 工作台 / Showcase / 小程序;旧包必须仍可打开 |

> 被这些决策覆盖的旧做法(不要再用):「Birthday 即模板」「Holo 即风格」「全程序化为卖点」「AI 只选色」「手工填 Card ID」「所有卡强制出现固定字段」。

---

## Phase 0 — 冻结基线 ✅ 2026-09-16
**做了什么**:给当前可运行版本打标签,作为 V2 重构的安全回退点。
- `git tag v1-birthday-final`(含当时的全部可运行能力:五套模板、材质系统、DL2 三套语言、工作台、小程序、卡包)
- 运行方式:需要回退时 `git checkout v1-birthday-final`
**测试**:标签已推送;工作区干净。
**已知问题**:此标签对应的工作台 UI 仍是 v1(6 个模板 + 字段表单),V2 将重构该界面。

---

## Phase 1 — 文档统一 + 旧文档归档 ✅ 2026-09-16
**做了什么**
| 文件 | 动作 |
|---|---|
| `docs/V2-主规范.md` | **新建** —— V2 唯一产品规范(定位/四维度/四语言/工作流/字段/正背面/AI 架构/材质/包契约/硬约束/QA/阶段) |
| `docs/V2-进度日志.md` | **新建** —— 本文件 |
| `README.md` | **重写** —— 不再把项目定义成 "Birthday Card Generator" |
| `卡片工坊-介绍.md` | **重写**为 V2 版(旧版归档) |
| `archive/v1-birthday/docs/` | 移入:`Birthday系列-设计方案.md`、`数字收藏卡项目_AI完整开发提示词-当前版.docx`、`卡片工坊-介绍-v1.md` |
| `archive/v1-birthday/legacy-cards/` | 移入早期残留 `night-premium/`、`night-user/`(已确认**无任何代码引用**) |

**为什么**:旧文档继续被当作现行规范,是"开发跑偏"的最大来源(方案文档 §12 明确列为重点)。
**运行方式**:纯文档变更,无运行时影响。
**测试**:`grep` 确认 `night-premium/night-user` 无代码引用;归档后工作台/小程序功能未改动。
**已知问题**:
- `card-studio/使用说明.md` 描述的仍是 **v1 界面**,需在 Phase 2 随 UI 重构一起重写(已在文件顶部加提示)。
- 旧的"AI 完整开发提示词"docx 已归档,新规范的"给开发 AI 的提示词"部分即为 `docs/V2-主规范.md` 全文。

---

## 顺带修复(与文档方向无关,但影响日常出卡)✅ 2026-09-16
**问题**:`make_static_card` 偶发 `OSError: image file is truncated`(worker 日志中出现 2 次),静态卡出不来。
**根因**:图层写入**非原子** —— 读图方可能在 PNG 写到一半时读到半截文件。
**改动**:
- `card-studio/birthday_system.py`:新增 `_save_atomic()`,`source.png` 与**全部五层**改为「先写 `<name>.tmp.png` 再 `os.replace`」
- `card-studio/make_static_card.py`:新增 `load_layer()`(5 次退避重试 + 真正解码),`static.png` 改原子写
- `card-studio/render_previews.py`:新增 `safe_open()`
**测试**:逐项实测通过;**并发压力测试**(3 次重写图层 × 6 次出静态图并行)→ **0 错误**。
**已知问题**:无。

---

## 下一步(按主规范 §13)
| 阶段 | 内容 | 状态 |
|---|---|---|
| **Phase 2** | 工作台 UI 重构:订单 → AI 分析 → 提案 → 精修 → QA → 导出 | ⬜ 待开始 |
| Phase 3 | PORTRAIT / CYBER / EDITORIAL / MEMORY 各 1 个标杆款 | ⬜ |
| Phase 4 | Photo Analyzer + PhotoDNA + AI Art Director(3~4 提案) | ⬜(DL2 已有 PhotoDNA 与选色基础,需升级为"提案") |
| Phase 5 | AI Visual Critic + 自动修正 | ⬜ |
| Phase 6 | Schema v2 / Package / Showcase 收敛 | ⬜(建议放在 Phase 3-4 之后) |
| Phase 7 | 小程序兼容验证 | ⬜ |

**Phase 2 开始前需要明确的**:
1. 工作台首页是否**完全隐藏**五套 Birthday(降级为"兼容/历史"入口)?还是保留一个入口?
2. 订单模型是否需要"客户"概念(客户名/订单号),还是维持"每次生成一张卡"?
3. QA 步骤是**人工勾选**还是**AI Visual Critic 自动出报告 + 人工确认**?

---

## 追加记录模板(复制使用)
```
## Phase N — <标题> ✅/⬜ YYYY-MM-DD
**做了什么**:<文件 + 动作>
**为什么**:<对应主规范哪一条>
**运行方式**:<命令 / 入口>
**测试**:<命令 + 结果>
**已知问题**:<...>
```
