<!-- 卡牌页: 白底 · 卡片居中 · 拖动/陀螺仪/翻面 · 卡下方只留卡号与日期 -->
<view class="page" style="background:{{bg}}">
  <view style="height:{{statusBar}}px"></view>

  <view class="bar-in">
    <view class="ico back" bindtap="goBack"></view>
    <view class="bar-right">
      <view class="ico save" bindtap="onSave"></view>
      <button class="share-btn" open-type="share">
        <view class="ico share"></view>
      </button>
    </view>
  </view>

  <block wx:if="{{card}}">
    <view class="stage-wrap" style="padding: {{padT}}px {{padX}}px 0">
      <view class="enter {{ready ? 'in' : ''}}" style="{{enterStyle}}">
        <holo-card card="{{card}}" />
      </view>
    </view>

    <!-- 卡下方只留身份信息: 卡号 + 纪念日期。
         寄语/署名/拥有者这些字段没有删, 它们仍然在卡面与卡背上;
         这里不重复显示, 页面才是干净的。 -->
    <view class="meta {{ready ? 'in' : ''}}">
      <view class="latin m-id">{{card.displayId}}<text class="sep">·</text>{{card.dateText}}</view>
    </view>

    <view class="hint {{hintOff ? 'off' : ''}}">拖动旋转 · 点击翻面</view>
  </block>

  <!-- 找不到 / 连不上 / 数据层出错: 三种说三种话, 不装成"内容为空" -->
  <view class="missing" wx:else>
    <block wx:if="{{failed}}">
      <view class="missing-t">卡牌数据读取出错</view>
      <view class="missing-s">看一下控制台的报错信息</view>
    </block>
    <block wx:elif="{{offline}}">
      <view class="missing-t">暂时连不上</view>
      <view class="missing-s">网络或云端环境有问题，稍后再试一次</view>
    </block>
    <block wx:else>
      <view class="missing-t">这张卡不在这里</view>
      <view class="missing-s" wx:if="{{missing}}">它可能还没做好，或者链接不对</view>
    </block>
    <view class="missing-btn" wx:if="{{failed || offline}}" bindtap="onRetry">重试</view>
  </view>

  <!-- 保存用画布(不在屏幕上显示) -->
  <canvas type="2d" id="export" class="export-canvas"></canvas>
</view>
